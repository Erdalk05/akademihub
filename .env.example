# Next.js Environment Variables Template
# Copy this file to .env.local and fill in your actual values

# ========== PUBLIC ENVIRONMENT VARIABLES ==========
# These are visible in the browser, so don't put secrets here

NEXT_PUBLIC_APP_NAME=AkademiHub
NEXT_PUBLIC_APP_VERSION=1.0.0

# Supabase (PUBLIC KEYS)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key_here

# ========== SECRET ENVIRONMENT VARIABLES ==========
# These must NEVER be committed to version control
# They are only used on the server side

# Supabase Service Role (KEEP SECRET!)
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/akademihub

# OpenAI
OPENAI_API_KEY=sk-... (from https://platform.openai.com/account/api-keys)

# Email (Resend)
RESEND_API_KEY=re_... (from https://resend.com/api-keys)

# SMS (Twilio)
TWILIO_ACCOUNT_SID=AC... (from https://console.twilio.com)
TWILIO_AUTH_TOKEN=your_auth_token_here
TWILIO_PHONE_NUMBER=+1234567890 (Your Twilio phone number)

# Node Environment
NODE_ENV=production

# ========== OPTIONAL CONFIGURATION ==========

# Analytics
NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=G-... (optional)

# Sentry Error Tracking
SENTRY_DSN=... (optional)

# Custom Domain
NEXT_PUBLIC_DOMAIN=yourdomain.com

# API Rate Limiting
API_RATE_LIMIT=100 (requests per minute)

# Email Configuration
SMTP_HOST=smtp.resend.com
SMTP_PORT=465
SMTP_USER=default
SMTP_PASS=$RESEND_API_KEY

# ========== INSTRUCTIONS ==========
# 1. Duplicate this file: cp .env.example .env.local
# 2. Fill in all required values (those without defaults)
# 3. Keep .env.local in .gitignore (it is!)
# 4. On production (Vercel/Railway), use dashboard to set these
# 5. NEVER push .env.local to GitHub

